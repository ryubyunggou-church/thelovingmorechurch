# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- **일반 방문자 / 교인** — 로그인·회원가입 없이 사이트를 열람한다. 예배 시간, 교회 소식, 오시는
  길, 교육부서·선교사역 정보를 확인하는 것이 주된 목적. 고령 교인 이용 비중이 높은 편이나,
  접근성 관련 별도 공식 기준은 채택하지 않기로 확인함(일반적인 반응형·가독성 수준 유지).
- **관리자(최고관리자 1명 + 부관리자 최대 3명)** — Firebase Auth로 로그인 후, 방문자와 동일한
  화면 위에서 인라인 편집 UI(연필 아이콘 → 폼/모달)로 콘텐츠를 직접 CRUD한다. 콘텐츠 편집
  권한은 최고관리자·부관리자 동등하며, 부관리자 초대/해제만 최고관리자 전용 권한이다.

## Product Purpose

대한예수교장로회 사랑하는교회의 공식 홈페이지. 실질적으로 정적 콘텐츠 사이트이며, 모든 콘텐츠
변경은 자체 제작한 관리자 인라인 편집 모드를 통해 이루어진다. 별도 대시보드 없이, 관리자가 로그인
상태에서 실제 방문자용 페이지를 그대로 보면서 편집 가능한 블록을 바로 수정한다. DB 로직이
필요한 곳은 교회소식 게시판이 유일하며, 나머지는 관리자가 직접 입력하는 구조화된 정적 콘텐츠다.

## Positioning

일반적인 교회 홈페이지가 외부 빌더(Wix/Imweb 등) 템플릿이나 별도 백오피스 대시보드를 쓰는 것과
달리, 이 사이트는 방문자 화면 위에서 그대로 편집하는 인라인 편집 시스템을 자체 구축했다 — 관리자가
별도 관리자 페이지를 오가지 않고, 보이는 화면 그대로 콘텐츠를 관리한다.

## Operating Context

- SPA(React Router 7, 서브메뉴 없는 대메뉴 7개: HOME/교회소개/예배안내/교육부서/선교사역/
  교회소식/오시는길), 탭 기반 하위 콘텐츠(교회소개 3탭, 교육부서 4탭, 선교사역 2탭)는 URL
  세그먼트 없이 단일 페이지 상태 전환.
- 게시판(교회소식)만 상세 라우트(`/news/:id`)를 가짐.
- 관리자 인증은 Firebase Auth 이메일/비밀번호 + Firestore `admins/{uid}` 문서 존재 여부로
  이중 검증. 콘텐츠 저장은 Firestore 즉시 반영.
- 이미지 업로드는 공용 파이프라인(`uploadMediaFile()`)을 거쳐 자동 리사이즈(긴 변 1600px)·WebP
  재인코딩(GIF·SVG 제외) 후 Firebase Storage에 저장.
- 리치텍스트(교회소식 본문, 팝업 텍스트형)는 Tiptap 에디터로 작성하고 클라이언트(`lib/
  sanitize.ts`)와 서버(Cloud Function `sanitizeNewsOnWrite`) 양쪽에서 DOMPurify로 sanitize.
- 사이트 팝업 기능: 날짜 지정 노출, 콘텐츠 타입(이미지/PDF/텍스트), 위치·우선순위 지정, 다중
  팝업 스택 순차 노출, 모바일 비노출.

## Capabilities and Constraints

- React 19 + Vite + TypeScript, SSR 미채택(SPA) — SEO는 react-helmet-async 라우트별 메타태그
  주입 + `public/sitemap.xml`/`robots.txt` 수동 관리로 보완.
- 백엔드: Firebase Auth / Firestore / Storage / Hosting / Cloud Functions(2세대, Node 24).
- 상태 관리: Zustand. UI 컴포넌트: Radix UI 기반 shadcn 스타일 커스텀 컴포넌트, Tailwind CSS
  v4.
- 파일당 코드량 가이드라인: 250~300줄 초과 시 분리 검토(`CONTRIBUTING.md` 명시).
- 참고 사이트 irijc.co.kr(이리중앙교회)은 4가지 요소만 부분 참고(Hero 풀스크린 슬라이더 상호작용,
  퀵링크 아이콘·카드 패턴, 이미지 배경+텍스트 링크 블록, 브레드크럼+본문 컨테이너형 서브페이지
  레이아웃) — 메가메뉴·서브메뉴 구조나 게시판 UI 등 나머지는 참고 대상이 아니며 복제하지 않는다.
- 배포: GitHub Actions로 `main` push 시 lint→test→build 후 자동 배포(Firebase Hosting
  `tlmchurch.web.app`), PR 시 미리보기 채널 자동 생성.

## Brand Commitments

- 공식 명칭은 모든 노출 위치(탑바 사이트명, Footer, `<title>`/OG 타이틀, `siteSettings.
  siteName` 기본값)에서 **"대한예수교장로회 사랑하는교회"**로 고정. `<title>` 패턴:
  `{페이지명} | 대한예수교장로회 사랑하는교회`.
- 2026년 표어: "옛 사람을 벗고 새사람을 입자" — Hero 슬라이드 상단 배지·문구에 노출.
- 로고 자산: `prd/logo-image/`에 다국어(국/영문)·투명 배경 버전 다수 보유(Logo-00~02 계열,
  favicon-logo.png 등 원본 PSD 포함). 실제 파비콘은 `public/logo-image/favicon-logo.png`
  사용 중.

## Evidence on Hand

아래는 실제 콘텐츠로 확인됨(placeholder 아님, 그대로 게시 가능) — `prd/images/`:

- 담임목사 사진(`ryubyunggu.jpg`) 및 인사말 섹션 시안(`인사말섹션.png`).
- 교회 전경 사진 3종(`더사랑하는교회-전경-00/000/01.png`).
- 선교사역 사진: 몽골선교(`몽골선교.png`), 캄보디아선교(`캄보디아선교.jpg`) — 실제 선교지
  사진으로 확인됨(워터마크 없음, 다큐멘터리 톤).
- 교회주보 내피/외피(`교회주보-내피.jpeg`, `교회주보-외피.jpeg`), 오시는길 약도
  (`사랑하는교회-약도.png`), 2026년 표어 이미지(`2026년표어.png`).

**정정(2026-08-17, 리디자인 작업 중 발견)**: `student_mission.jpeg`는 실제 선교 사진이
아니라 오렌지 옆에 놓인 카드/문구류 제품 사진(삽화 카드 "Love God, Love Your Neighbor")이며,
`장학금지원.png`는 pngtree 워터마크가 선명한 저작권 스톡 이미지다. 두 파일 모두 "실제
콘텐츠"가 아니므로 사이트에 사용하지 않았다 — 리디자인에서 몽골/캄보디아 선교 사진으로
대체했고, 국내선교 2개 항목은 원래의 중립적 Unsplash 플레이스홀더로 되돌렸다. 실제 국내
학생선교·장학금지원 사진과 사역자(부목사·전도사 등) 프로필 사진은 아직 없음 — 관리자가
추후 업로드해야 한다.
- 외부 협력 기관 로고(실사용): GMS 총회세계선교회, 남경기노회, 대한예수교장로회 총회 —
  `prd/logo-image/`에 투명 배경 버전 보유. 홈 바로가기 카드 2에서 gapck.org/gms.kr 등 외부
  링크로 연결.

## Product Principles

1. **방문자는 로그인 없이, 관리자는 보이는 화면 그대로 편집한다.** 회원가입/방문자 로그인 없는
   완전 공개 열람 + 관리자만 인라인 편집 — 별도 대시보드로 우회하지 않는다.
2. **대메뉴만, 서브메뉴 없이.** 정보 구조는 7개 대메뉴로 고정하고 하위 콘텐츠는 탭 상태 전환으로
   처리 — 단, 검색 유입이 중요한 정적 콘텐츠(예배안내·오시는길)는 별도 라우트를 유지해 SEO를
   상쇄한다.
3. **참고 사이트는 부분 차용, 전체 복제 금지.** irijc.co.kr에서 가져오는 것은 4가지 상호작용
   패턴뿐이며 사이트 구조 전체를 옮기지 않는다.
4. **보안은 처음부터 설계에 포함한다.** 현재 공개 글쓰기 경로가 없어도, 게시판 확장(댓글/문의)을
   대비해 요청 제한·업로드 검증·서버측 sanitize를 1단계부터 적용한다.
5. **실제 교회 자료를 그대로 신뢰한다.** `prd/images/`의 선교·인사말·전경 사진은 실사용 콘텐츠이며
   재작업이나 대체 목업을 만들 필요가 없다.
